# Story 6.1: Local LLM Docker Setup

[← Back to Scope Matrix](../01_scope-matrix.md)

---

**Phase:** 6 - AI Features  
**Priority:** Critical  
**Status:** ⏳ Not Started  
**Dependencies:** None

---

## Description

Set up a Docker container running llama.cpp with OpenAI-compatible API for providing AI feedback to students. Works offline, supports CPU/GPU, zero code changes for model switching.

---

## Goals

### Functional
- ✅ Run completely offline (no external API calls)
- ✅ Works on Windows, macOS, Linux
- ✅ Works with or without GPU (auto-detection)
- ✅ OpenAI-compatible API
- ✅ Fast startup (< 30 seconds)
- ✅ Easy model upgrades (no Docker rebuild)
- ✅ Multiple models supported
- ✅ Zero code changes when switching models

---

## Technology Stack

| Component | Choice | Rationale |
|-----------|--------|-----------|
| Runtime | llama.cpp | Fast, efficient, no dependencies |
| API | llama-server | OpenAI compatible, proven |
| Container | Docker | Portable, isolated |
| Models | GGUF | Quantized, fast loading |
| Quantization | Q4_K_M | Balance speed/quality |
| Default Model | Qwen2.5-Coder-3B | Small, fast, code-aware |
| Client SDK | OpenAI SDK | Standard, no custom code |
| Transport | HTTP | Simple, reliable |

---

## Architecture

### Docker Container

**Contains ONLY:**
- Ubuntu base image
- llama.cpp binary
- startup script
- runtime configuration

**Does NOT contain:**
- ❌ Python
- ❌ Node.js
- ❌ Ollama
- ❌ Models (mounted as volumes)

**Ports:**
- `8001` - OpenAI-compatible API

**Volumes:**
- `/models` - Model files
- `/config` - Runtime configuration
- `/cache` - KV cache
- `/logs` - Execution logs

**Health Check:**
- `GET /health` - Returns status and model info

---

## Directory Structure

```
lms/
├── docker/
│   ├── Dockerfile
│   ├── docker-compose.yml
│   └── start.sh
├── models/
│   ├── manifest.json
│   ├── active.gguf (symlink or copy)
│   ├── qwen2.5-coder-3b-q4.gguf
│   └── qwen2.5-coder-1.5b-q4.gguf
├── config/
│   └── runtime.json
├── logs/
├── cache/
└── app/ (placeholder)
```

---

## Dockerfile Specification

```dockerfile
FROM ubuntu:22.04

WORKDIR /app

# Install minimal dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    git \
    wget \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

# Clone and build llama.cpp
RUN git clone https://github.com/ggerganov/llama.cpp.git && \
    cd llama.cpp && \
    make

# Copy startup script
COPY start.sh /app/start.sh
RUN chmod +x /app/start.sh

# Copy runtime config
COPY config/runtime.json /app/runtime.json

EXPOSE 8001

HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
    CMD curl -f http://localhost:8001/health || exit 1

CMD ["/app/start.sh"]
```

---

## Docker Compose

```yaml
version: '3.8'

services:
  llm-server:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: llm-server
    ports:
      - "8001:8001"
    volumes:
      - ./models:/models:ro
      - ./config/runtime.json:/app/runtime.json:ro
      - ./cache:/cache
      - ./logs:/logs
    environment:
      - MODEL_PATH=/models
      - CONFIG_PATH=/app/runtime.json
      - CACHE_PATH=/cache
      - LOG_PATH=/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
```

---

## Startup Flow

```
docker-compose up
    ↓
Container starts
    ↓
start.sh executes
    ↓
Read runtime.json
    ↓
Read manifest.json
    ↓
Load active.gguf
    ↓
Start llama-server on port 8001
    ↓
/health returns success
    ↓
Ready for requests
```

---

## Runtime Configuration

**File: `config/runtime.json`**

```json
{
  "model": "active.gguf",
  "host": "0.0.0.0",
  "port": 8001,
  "threads": "auto",
  "context_size": 8192,
  "temperature": 0.2,
  "top_p": 0.9,
  "top_k": 40,
  "max_tokens": 150,
  "parallel": 2,
  "cache_type_k": "q8_0",
  "cache_type_v": "q8_0"
}
```

**Auto-detection:**
- Metal (macOS with GPU)
- CUDA (NVIDIA)
- Vulkan (AMD)
- CPU (fallback)

---

## Model Management

### Model Location

**NEVER package models inside Docker image.**

**Always mount `/models` volume:**

```
/models/
├── active.gguf (symlink or copy)
├── qwen2.5-coder-3b-v1.gguf
├── qwen2.5-coder-3b-v2.gguf
└── qwen2.5-coder-1.5b.gguf
```

Changing model: Update `active.gguf` symlink/copy → Restart container

**No Docker rebuild needed.**

### Model Manifest

**File: `models/manifest.json`**

```json
{
  "defaultModel": "qwen2.5-coder-3b-v2",
  "models": [
    {
      "id": "qwen2.5-coder-3b-v2",
      "version": "2.0",
      "filename": "qwen2.5-coder-3b-v2.gguf",
      "size_mb": 2048,
      "sha256": "abc123...",
      "downloadUrl": "https://company-cdn.com/models/qwen2.5-coder-3b-v2.gguf",
      "releaseDate": "2024-07-09",
      "description": "Latest Qwen coder model, optimized for code review"
    },
    {
      "id": "qwen2.5-coder-1.5b",
      "version": "1.0",
      "filename": "qwen2.5-coder-1.5b.gguf",
      "size_mb": 1024,
      "sha256": "def456...",
      "downloadUrl": "https://company-cdn.com/models/qwen2.5-coder-1.5b.gguf",
      "releaseDate": "2024-06-15",
      "description": "Lightweight model for faster feedback"
    }
  ]
}
```

---

## API Endpoint

### OpenAI-Compatible Chat API

**Endpoint:** `POST /v1/chat/completions`

**Request:**
```json
{
  "model": "qwen",
  "messages": [
    {
      "role": "system",
      "content": "You are an experienced Python reviewer. Never reveal the correct solution. Only provide hints. Explain mistakes clearly. Praise correct approaches."
    },
    {
      "role": "user",
      "content": "Review this code: def foo(): return 42"
    }
  ],
  "temperature": 0.2,
  "max_tokens": 150,
  "top_p": 0.9
}
```

**Response:**
```json
{
  "id": "chatcmpl-abc123",
  "object": "chat.completion",
  "created": 1720598400,
  "model": "qwen2.5-coder-3b",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "Your solution correctly implements the function. Consider adding docstring and type hints."
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 45,
    "completion_tokens": 23,
    "total_tokens": 68
  }
}
```

**Client Code (Python):**
```python
from openai import OpenAI

client = OpenAI(
    api_key="dummy",  # Not needed, local server
    base_url="http://localhost:8001/v1"
)

response = client.chat.completions.create(
    model="qwen",
    messages=[
        {
            "role": "system",
            "content": "You are an experienced Python reviewer."
        },
        {
            "role": "user",
            "content": "Review this code..."
        }
    ],
    temperature=0.2,
    max_tokens=150
)

print(response.choices[0].message.content)
```

---

## Health Endpoint

**Endpoint:** `GET /health`

**Response:**
```json
{
  "status": "healthy",
  "model": "qwen2.5-coder-3b",
  "version": "2.0",
  "loaded": true,
  "uptime_seconds": 12345,
  "context_size": 8192,
  "threads": 8,
  "gpu_available": true
}
```

---

## Logging

Log the following for diagnostics:

```
[TIMESTAMP] Model loaded: qwen2.5-coder-3b
[TIMESTAMP] Context size: 8192
[TIMESTAMP] Threads: 8
[TIMESTAMP] GPU: available (CUDA)
[TIMESTAMP] Listening on 0.0.0.0:8001

[TIMESTAMP] Request: prompt_tokens=45
[TIMESTAMP] Generation: completion_tokens=23
[TIMESTAMP] Latency: 234ms
[TIMESTAMP] Generation speed: 98 tokens/sec
[TIMESTAMP] Status: success
```

---

## Performance Metrics

Measure and log:

```
Time To First Token (TTFT)
Total Latency
Generation Tokens/sec
Prompt Tokens/sec
Memory Usage (MB)
CPU Usage (%)
GPU Usage (%)
Concurrent Requests
```

Store in `/logs/metrics.json` for diagnostics.

---

## Tasks

- [ ] Create Dockerfile with llama.cpp
- [ ] Build and test Docker image
- [ ] Create docker-compose.yml
- [ ] Create start.sh script
- [ ] Create runtime.json configuration
- [ ] Create models/manifest.json
- [ ] Download/obtain Qwen2.5-Coder-3B GGUF
- [ ] Test container startup
- [ ] Verify /health endpoint works
- [ ] Test /v1/chat/completions endpoint
- [ ] Test with OpenAI Python SDK
- [ ] Test GPU auto-detection
- [ ] Test CPU-only mode
- [ ] Create documentation for model setup
- [ ] Create documentation for model switching
- [ ] Test on Windows, macOS, Linux

---

## Acceptance Criteria

- [x] Container runs on port 8001
- [x] Supports CPU and GPU (auto-detection)
- [x] OpenAI-compatible API endpoint
- [x] Model files mounted as volumes (not in image)
- [x] Model switching without Docker rebuild
- [x] Health endpoint returns correct status
- [x] Chat completions endpoint works
- [x] Proper error handling and logging
- [x] Works on Windows, macOS, Linux
- [x] Runtime configuration via runtime.json
- [x] Model manifest.json for versioning

---

## Success Criteria

✅ Docker container starts successfully  
✅ `/health` returns healthy status  
✅ `/v1/chat/completions` accepts requests and returns responses  
✅ OpenAI Python SDK works without modifications  
✅ Model can be switched by updating active.gguf  
✅ No Python/Node.js in container  
✅ Auto-detects GPU (if available)  

---

## Notes

- **No External Dependencies:** Container is completely self-contained (except mounted volumes)
- **Model Updates:** Story 6.2 will handle automatic model updates
- **Advanced Features:** Story 6.3 will handle prompt caching, streaming, benchmarking
- **Prompt Management:** Language-specific prompts will be defined in separate story (API integration)

---

## Related Stories

- **6.2:** Model Management & Auto-Updates
- **6.3:** Advanced Features (Caching, Streaming, Benchmarking)
- **6.4:** LLM Feedback API Integration (with /execute endpoint)

---

[← Back to Scope Matrix](../01_scope-matrix.md)
